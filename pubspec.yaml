name: mommyg
description: An emotionally supportive AI mother app

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_generative_ai: ^0.4.3
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.2.2
  google_fonts: ^6.1.0
  provider: ^6.0.5
  speech_to_text: ^6.1.1
  flutter_tts: ^4.0.2
  permission_handler: ^11.3.1
  intl: ^0.19.0
  fl_chart: ^0.68.0
  flutter_local_notifications: ^16.3.2
  timezone: ^0.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - .env